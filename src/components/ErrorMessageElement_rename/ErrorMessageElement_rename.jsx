import './ErrorMessageElement_rename.css';

// это отдельный компонент для вывода ошибок при работе с API — это может быть модальное окно или текстовое сообщение в соответствующих частях проекта, я еще не определилась с его видом, поэтому он в виде скелета с пометкой ренейминга
function ErrorMessageElement_rename() {
  return (
    <div className="ErrorMessageElement_rename">

    </div>
  );
}

export default ErrorMessageElement_rename;